#!/usr/bin/python

from opencv import cv

if __name__ == "__main__":
  print """cvGetModuleInfo doesn't appear to work, but in any case it seems
  support for IPP is reduced in OpenCV 2.0 and performance gains are minimal
  in 2.0 or later."""
