#!/usr/bin/python

import cv2

if __name__ == "__main__":
  print """cv.GetModuleInfo doesn't appear to work, but in any case it seems
  support for IPP is reduced in OpenCV 2.0+ and performance gains are minimal
  in 2.0+ or later."""
